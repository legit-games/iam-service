package server

// TestMain is commented out to avoid test hanging issues during development
// and unused import errors
/*
import (
	"os"
	"testing"
)

func TestMain(m *testing.M) {
	// No-op: run tests directly
	os.Exit(m.Run())
}
*/
